<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
                            http://www.springframework.org/schema/beans/spring-beans.xsd
                            http://www.springframework.org/schema/mvc
                            http://www.springframework.org/schema/mvc/spring-mvc.xsd
                            http://code.alibabatech.com/schema/dubbo
                            http://code.alibabatech.com/schema/dubbo/dubbo.xsd
                            http://www.springframework.org/schema/context
                            http://www.springframework.org/schema/context/spring-context.xsd">


    <!-- 指定应用名称 -->
    <dubbo:application name="health_service_provider"/>
    <!--指定暴露服务的端口，如果不指定默认为20880-->
    <dubbo:protocol name="dubbo" port="20888"/>
    <!--指定服务注册中心地址-->
    <dubbo:registry address="zookeeper://120.27.227.78:2181"/>

    <!--批量扫描，发布服务-->
    <dubbo:annotation package="com.dou.service"/>


    <bean id="freemarkerConfig"
          class="org.springframework.web.servlet.view.freemarker.FreeMarkerConfigurer">
        <!--指定模板文件所在目录-->
        <property name="templateLoaderPath" value="/WEB-INF/ftl" />
        <!--指定字符集-->
        <property name="defaultEncoding" value="UTF-8" />
        <property name="freemarkerSettings">
            <props>
                <!--用于解决前端报空指针问题-->
                <prop key="classic_compatible">true</prop>
            </props>
        </property>
    </bean>
    <!--加载属性文件，后期在java代码中会使用到属性文件中定义的key和value,这是主要设置.-->
    <context:property-placeholder location="classpath:freemarker.properties" file-encoding="utf-8"/>
    <!--如果有多个property需要加载,只能写在一个地方,可以如下使用-->
<!--<context:property-placeholder
location="classpath:freemarker.properties,
classpath:redis.properties" file-encoding="UTF-8"/>-->
</beans>